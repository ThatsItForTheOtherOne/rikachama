FROM alpine:latest

RUN apk update && apk upgrade --no-cache && \
    apk add --no-cache imagemagick ffmpeg mupdf-tools && \
    fc-cache -f

ENV XDG_CACHE_HOME=/tmp/fc

COPY entrypoint.sh /entrypoint.sh
RUN chmod +x /entrypoint.sh

ENTRYPOINT ["/entrypoint.sh"]
